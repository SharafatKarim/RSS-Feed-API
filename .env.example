# Comma-separated list of allowed origins. Use * to allow all (not recommended for production).
CORS_ALLOWLIST=http://localhost:3000,http://localhost:8000
PORT=3001
# Fetch timeout in milliseconds for upstream requests (default: 8000)
FETCH_TIMEOUT_MS=8000
